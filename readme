Prerequisites:
- maven, jdk 8
- good understanding of springboot
 
- mvn clean compile install -DskipTests

Instructions to run as spring boot project
1.	Import the project as existing maven project in eclipse
2.	Run as maven project with command  "mvn clean compile install"
3.	Locate executable JAR file (HistoricalTimeline-app-0.0.1.jar) inside /target folder 
4.	Run command "java -jar HistoricalTimeline-app-0.0.1.jar 
    (This will launch inbuilt tomcat container and execute the application)